import { defineAppConfig } from "nuxt/app";

// app.config.ts
export default defineAppConfig({
  ui: {
    colors: {
      primary: "purple",
      secondary: "blue",
      accent: "pink",
      neutral: "zinc",
    },
  },
});
